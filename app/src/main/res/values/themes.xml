<resources xmlns:tools="http://schemas.android.com/tools">
    <style name="Theme.GameSudoku" parent="Theme.Material3.Dark.NoActionBar">
        <item name="colorPrimary">@color/purple_200</item>
        <item name="colorPrimaryContainer">@color/purple_700</item>
        <item name="colorOnPrimary">@color/black</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <item name="android:windowLightStatusBar">false</item>
        <item name="android:windowBackground">@drawable/bg_gradient_warm</item>
        <item name="android:windowDrawsSystemBarBackgrounds">true</item>
        <item name="android:windowLayoutInDisplayCutoutMode">shortEdges</item>
        
        <!-- Override button styles to use custom backgrounds -->
        <item name="android:buttonStyle">@style/ActionButtonStyle</item>
        <item name="materialButtonStyle">@style/ActionButtonStyle</item>
    </style>
</resources>
